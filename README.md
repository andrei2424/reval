## Calculator

### Terminal 1
```shell
cd client
npm install && npm run production
composer install
php artisan serve
```

### Terminal 2
```shell
cd service
composer install
symfony server:start
```
